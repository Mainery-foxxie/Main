print(identifyexecutor())
if string.find(identifyexecutor(), "Arceus X") or
   string.find(identifyexecutor(), "Xeno") or
   string.find(identifyexecutor(), "Solara") or
   string.find(identifyexecutor(), "Seliware") or
   string.find(identifyexecutor(), "Elternia") then
    print("You executor doesn't support this function")
else
    print("hi")
end
